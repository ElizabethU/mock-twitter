<% unless @tweets.count == 0 %>
  <% @tweets.each do |tweet| %>
    <%= tweet.user %><br>
    <%= tweet.message %><br><hr>
  <% end %>
<% else %>
  <p>Does no one care about a hack imitation twitter site?<p>
<% end %>